.venv/
__pycache__/
*.pyc
# Regenerable surrogate training data (python -m ml.surrogate.generate_data)
ml/surrogate/training_data.parquet
# Playwright CLI snapshots and screenshots
.playwright-cli/
# Machine-specific preview server config (absolute paths)
.claude/launch.json
# Local development database (python -m db.local)
.localdb/
# Signed-in browser state saved by web/e2e/auth.setup.ts
web/e2e/.auth/
# Local secrets (PLAN.md 1.7): keys live in Render, Vercel and GitHub secrets
.env
.env.*
!.env.example
