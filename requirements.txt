scipy>=1.18.1
numpy>=2.5.3
pandas>=3.0.6
numpy-financial>=1.1.0
openpyxl>=3.1.5         # Excel downloads (api/routers/export.py)
fastapi>=0.141.1       # HTTP API (api/)
uvicorn>=0.53.0        # serves the API
requests>=2.34.2        # optional: SEC EDGAR autofill in forecasting mode
sentry-sdk>=2.69.2          # error tracking, on when SENTRY_DSN is set (api/observability.py)
sqlalchemy>=2.0.54,<2.1     # database layer (db/), Postgres on Neon
alembic>=1.20.0            # schema migrations (db/migrations)
psycopg[binary]>=3.3.6     # Postgres driver
pyjwt[crypto]>=2.14.0      # verifies Clerk session tokens locally (api/auth.py)
cryptography>=50.0.1         # AES-256-GCM for database backups (ops/encryption.py); pyjwt[crypto] pulls it in too
tzdata>=2026.4           # IANA time zones for account profiles (slim images have none)
