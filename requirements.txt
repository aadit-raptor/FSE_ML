scipy>=1.11
numpy>=1.24
pandas>=2.0
numpy-financial>=1.0
openpyxl>=3.1         # Excel downloads (api/routers/export.py)
fastapi>=0.141       # HTTP API (api/)
uvicorn>=0.30        # serves the API
requests>=2.31        # optional: SEC EDGAR autofill in forecasting mode
sentry-sdk>=2.0          # error tracking, on when SENTRY_DSN is set (api/observability.py)
sqlalchemy>=2.0,<2.1     # database layer (db/), Postgres on Neon
alembic>=1.13            # schema migrations (db/migrations)
psycopg[binary]>=3.2     # Postgres driver
pyjwt[crypto]>=2.9      # verifies Clerk session tokens locally (api/auth.py)
cryptography>=42         # AES-256-GCM for database backups (ops/encryption.py); pyjwt[crypto] pulls it in too
tzdata>=2024.1           # IANA time zones for account profiles (slim images have none)
